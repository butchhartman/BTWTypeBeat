Allow setting window size on creation
X - Remove the debug popup describing the opengl version and replace it with a print function
X +Turn off compiler warnings when GWL is not the main project
X - Have the main CMakeLists.txt include the win32, mac, or linux subdirectory based on what the target compile platform is
Define an OpenGL loader function
X - Get time function